#include <iostream>
using namespace std;

int arr[10001] = {0};

int main() {
    int n, x, max = 0;
    cin.tie(NULL);
    cin >> n;

    for (int i = 0; i < n; i++) {
        scanf("%d", &x);
        arr[x - 1]++;  // arr[x]의 값을 ++;
        if (x > max)
            max = x;
    }

    for (int i = 0; i < max; i++)
        while (arr[i] > 0) {
            printf("%d\n", i + 1);
            arr[i]--;
        }
}
